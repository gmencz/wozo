// *** GENERATED BY PRISMIX :: DO NOT EDIT ***
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Account {
  id    String @id
  email String
  user  User?  @relation(name: "AccountToUser")
}

model User {
  id        String  @id
  account   Account @relation(name: "AccountToUser", fields: [accountId], references: [id])
  accountId String
}
